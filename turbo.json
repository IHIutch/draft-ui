{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "build": {
      "outputs": ["storybook-static/**", ".next/**", "!.next/cache/**"]
    },
    "storybook": {},
    "lint": {},
    "deploy": {
      "dependsOn": ["build", "lint"]
    }
  }
}
