---
title: Radio Group
description: A radio group allows a user to select a single item from a list of mutually exclusive options.
isComponent: true
---

## Installation

1. Copy and paste the following code into your project.

This is the **`RadioGroup`** primitive. You can place it in a file located at `components/ui/RadioGroup.tsx`.

## Component Source

Copy and paste the code below into your project to install this component.

<ComponentSource src="../../packages/ui/src/RadioGroup.tsx" />

## Examples

### Default

The default **`RadioGroup`** uses orientation `vertical`.

<ComponentExample name="RadioGroup" story="default" />

### Horizontal

<Callout type="warning">
  Unlike **`CheckboxGroup`**, **`RadioGroup`** does support `aria-orientation`.
  So that is used to style the layout.
</Callout>

Set the `orientation` prop to `horizontal` to use a horizontal **`RadioGroup`**.

<ComponentExample name="RadioGroup" story="horizontal" />
